# Athena

Single native macOS/MLX daemon providing LLM chat, text embeddings, and audio/video transcription/diarization/speaker-embeddings — all governed by one Metal memory budget. Passive oracle: answers inbound requests only.

## Architecture

- Swift package (`Package.swift`, `Sources/`) targeting macOS on Apple Silicon. Build requires full Xcode (Command-Line Tools alone cannot compile the MLX Metal shaders).
- HTTP daemon on `127.0.0.1:7447` by default. Bearer-token RBAC; auth disabled in loopback dev mode.
- Modules: `AthenaLLM`, `AthenaEmbedding`, `AthenaTranscription` (each in `Sources/`).
- Cross-platform client CLI lives under `clients/` (Swift package, builds on Linux/Windows).
- Rust shim in `rust-shim/` for components not yet Swift-native.

## Canonical pipelines

- **All HTTP routes are defined in `Sources/athena/Server/OpenAPISpec.swift`.** Do not add or modify a route without updating that spec in the same edit. The daemon serves it verbatim at `GET /openapi.json`.
- **All errors return the envelope `{"error":{"message","type","code"}}`.** Never invent ad-hoc error shapes.
- **Outbound network is forbidden except model-weight fetches from Hugging Face** (and the opt-in remote-syslog sink). The "passive oracle" rule is binding — no result webhooks, no billing callbacks, no telemetry pings.
- **Native DTOs for the `/api/*` dialect live in `Sources/athena/Server/NativeAPIDTO.swift`.** Do not duplicate `/v1/*` OpenAI shapes there.

## Public surface

Athena is a passive oracle. Consumers interact only via HTTP.

**Self-describing**: `GET /openapi.json` returns the full OpenAPI 3.0.3 spec for both dialects. Always reachable, no auth required. For static reading from another repo, load `Sources/athena/Server/OpenAPISpec.swift`.

**Two HTTP dialects**:

| Surface | Shape | Use for |
|---|---|---|
| `/v1/*` | Inference + data plane | OpenAI-compatible drop-ins **and** Athena-native extensions under the `/v1` namespace (see the compatibility rule below). |
| `/api/*` | Athena native (control) | Daemon **control plane** — model-store, RBAC, allowlist, lifecycle, audit, usage, logs, cache. **NOT inference** — `/api/chat`+`/api/embed` are deprecated (ADR 013); new inference features go to `/v1` only. |

**`/v1` compatibility rule (binding).** `/v1/*` is **not uniformly
OpenAI-compatible** — it is the inference + data surface, and only a subset is a
literal OpenAI drop-in. **Whenever a new `/v1/*` surface is added that is not
strictly OpenAI-compatible (no equivalent OpenAI endpoint, or an
OpenAI-shaped response over a non-OpenAI route), it MUST be marked as
Athena-native** in the same edit: (a) note it in the introducing ADR, (b) say so
in its `OpenAPISpec.swift` operation `description`, and (c) tag the endpoint in
the list below. A consumer must be able to tell drop-in OpenAI from native
extension without reading the code. Reusing an OpenAI *response shape* (e.g.
verbose_json) for consumer convenience does **not** make a native route
OpenAI-compatible — the route's existence/semantics are what count.

**Stable `/v1/*` endpoints** (canonical list in `OpenAPISpec.swift`; **[oai]** =
OpenAI-compatible drop-in, **[native]** = Athena extension under `/v1`):

- `POST /v1/chat/completions` **[oai]** — streaming SSE, tool calls, JSON-schema structured output, `stop`/`seed`/`top_p`
- `POST /v1/chat/completions/count_tokens` **[native]** — exact pre-flight `{prompt_tokens}` for a chat body (same template/tokenizer as the request path, ADR 042); no OpenAI equivalent; no generation, no inference gate, not metered
- `POST /v1/messages/count_tokens` **[anthropic]** — the Messages-dialect analogue (`{input_tokens}`), same decoder + same counting core, so the two dialects report the same number for the same conversation (ADR 042 §4(a), deferral lifted 2026-07-25 for dialect parity)
- `POST /v1/embeddings` **[oai]**
- `POST /v1/audio/transcriptions` **[oai]** — word timestamps, SRT/VTT
- `POST /v1/video/transcriptions` **[native]** — demux a video's audio track → transcription (ADR 022); no OpenAI equivalent
- `POST /v1/audio/diarizations` **[native]** — no OpenAI equivalent
- `POST /v1/audio/embeddings` (speaker embeddings) **[native]** — no OpenAI equivalent
- `GET /v1/models`, `GET /v1/models/{id}` **[oai]** — plus two **Athena-native extension fields** (ADR 042): `context_length` (what the checkpoint advertises) + `max_prompt_tokens` (the ADR 030 prefill ceiling this daemon enforces), both omitted-when-nil
- _(ADR 025: `/v1/queue*` removed v0.10.203 — model lifecycle ops stream SSE on `POST /api/models/{pull,convert,prune}`; `/v1/vectors*` + `/v1/store*` removed v0.10.201.)_

**Native `/api/*`**: model-store and RBAC admin. Surface defined in `OpenAPISpec.swift` and `NativeAPIDTO.swift`.

**Auth**: `Authorization: Bearer <token>`. Each route requires a single RBAC permission. Loopback dev mode (no seeded users) opens every route.

**Diagnostics**: macOS unified log, subsystem `athena`. Off-box log shipping is operator-side — see `docs/logging.md`, `docs/logging-shipping.md`.

## Dependencies (consumed by this repo)

- Hugging Face — model weight fetches only. No other outbound dependencies.

## ADRs

Read `docs/decisions/` before architectural changes — particularly anything that would touch the passive-oracle rule, the OpenAPI spec, or the Metal memory governor.

- [`001-dflash-speculative-decoding.md`](docs/decisions/001-dflash-speculative-decoding.md) — **SUPERSEDED by ADR 028 (DFlash removed v0.10.217).** DFlash lossless speculative decoding for non-MTP targets (Gemma4-first), default-off; vendored from bstnxbt/dflash-mlx (Apache-2.0). M63.
- [`002-gemma4-moe-arch.md`](docs/decisions/002-gemma4-moe-arch.md) — Gemma4 MoE architecture support (26B-A4B; 128-expert), additive substrate delta reusing `SwitchGLU`, dense path byte-unchanged; unblocks ADR 001's M63.5 (DFlash on the MoE target). M64.
- [`003-rust-shim-panic-strategy.md`](docs/decisions/003-rust-shim-panic-strategy.md) — rust-shim FFI panic strategy: per-entry `catch_unwind` (crate stays `panic="unwind"`, NOT `abort`) so a hostile schema degrades to an error, not a daemon abort. Shipped M65.1 v0.10.117 (audit G1).
- [`004-nonloopback-tls-posture.md`](docs/decisions/004-nonloopback-tls-posture.md) — non-loopback auth-on plaintext is **warn-only** (loud startup warning + doctor finding), not fail-closed; no `--insecure` gate, client https deferred; login limiter (A3) keys on TCP peer IP, no XFF trust. M65 (audit A2/K1/K8/A3).
- [`005-remove-secrets-from-argv.md`](docs/decisions/005-remove-secrets-from-argv.md) — hard-remove `--password` from argv; prompt / `--password-stdin` / env only (breaking CLI change). M66 (audit B2/K7/K13).
- [`006-vector-store-owner-scoping.md`](docs/decisions/006-vector-store-owner-scoping.md) — add a nullable `owner` column to the vector store; query/get/delete owner-filtered, admin sees all, legacy NULL rows admin-only (additive migration). M66 (audit H5).
- [`007-api-metering-and-quotas.md`](docs/decisions/007-api-metering-and-quotas.md) — bring native `/api` metering (#8) + token-budget quotas (#9) into the program as their own milestone; NA8 single-resolution folds in. M69+ (audit NA8). **AMENDED 2026-07-25 / superseded in part by ADR 041:** #8 is **closed by obsolescence** — ADR 013/025/031 removed every native inference route (`/api/chat`, the `/api/embed` alias), so `/api/*` is control plane carrying zero model tokens and there is nothing left to meter (the ADR's `handleNativeEmbed` caveat names a function that no longer exists); NA8 shipped v0.10.140. #9 quotas is the only live item, designed in ADR 041.
- [`008-testable-server-seam.md`](docs/decisions/008-testable-server-seam.md) — extract the daemon's pure HTTP-server primitives (auth/ratelimit/concurrency/session/multipart/metrics/logging) into a new MLX-free `AthenaServerKit` library so they're unit-testable under `swift test`; chosen over `@testable import` of the `@main` executable (which would co-link the whole MLX/Metal graph). Pure refactor. Implemented M70.1 v0.10.144 (audit NA2/NB4).
- [`009-stub-decode-ci-tier.md`](docs/decisions/009-stub-decode-ci-tier.md) — **Accepted** (M70.2/.3, from v0.10.151): a stub-model CI tier that is a **control-flow / decision-algebra tier, not a numeric tier** — MLX can't evaluate *any* `MLXArray` under `swift test` (no metallib), so extract each decode path's MLX-free decision logic into pure-Swift static seams pinned by `./deploy/test.sh`; `MLXArray` numerics stay in `AthenaLLM` byte-unchanged. Resolves the L-cluster "CI blindness" (L1–L11, NC4/NC5/NC6).
- [`010-vision-input-vlm-chat.md`](docs/decisions/010-vision-input-vlm-chat.md) — **Accepted** (M71.1 v0.10.159 + M71.2 v0.10.160 shipped): add image input to chat by wiring the daemon to the substrate's already-implemented `MLXVLM.Gemma4` vision path (not a new encoder port); OpenAI `image_url` content-parts; base64/`data:` only (no outbound image fetch — passive-oracle preserved, 400 on `http(s)`); audio-in-chat deferred (substrate strips the audio tower, overlaps `/v1/audio/*`). Resolves issue #3. Plan: `docs/m71-vision-input-plan.md`.
- [`011-unified-memory-governor-as-thesis.md`](docs/decisions/011-unified-memory-governor-as-thesis.md) — **Accepted** (positioning): the **unified Metal memory governor is Athena's reason to exist**; audio/embeddings/vision/vectors/queue are *tenants* proving it across modalities; chat-GUI/model-discovery/download-UX are undifferentiated *tax* (maintain, don't out-polish LM Studio — a client, not a competitor). Coexist; **never compose at the inference layer** (two uncoordinated allocators on one Metal pool defeats the governor). Next milestone = **governor accounting truthfulness** (heartbeat RSS undercounts GPU). GPU-compute-extension governance model (in-process tenants vs. cooperative reservation) left **open**. Retire-tripwire: if audio + multi-tenant + near-ceiling concurrency lapse, retire for LM Studio + embeddings sidecar.
- [`012-vision-aware-convert.md`](docs/decisions/012-vision-aware-convert.md) — **Accepted** (M72.1 shipped v0.10.161): make `athena convert` vision-aware so `athena convert google/gemma-4-26B-A4B` produces a load/serve-compatible vision model. Route the convert load through `VLMModelFactory` (keep the `vision_tower`, not strip it); quantize the language model only (closure-form `quantize`, MoE mixed 4/8-bit) and leave the vision tower **full-precision** (no `.scales` ⇒ the `.scales`-driven loader skips it — matches `mlx-community/gemma-4-26b-a4b-it-4bit`); emit the matching per-layer quant config from one shared quant-rule. Serve side already validated (M71.2). Plan: `docs/m72-vision-convert-plan.md`. M72. **Amended v0.10.165**: the 8/4 mixed scheme is **gemma4-only** — `Gemma4QuantRule.mixedPrecision` gated on `model_type=="gemma4"`; other arches (`qwen3_5`, `qwen3_5_moe`) quantize uniformly at the global bits (the override else mis-fired on dense `mlp` / MoE `switch_mlp` experts, bloating "4-bit" Qwen converts to ~8-bit size); encoder-tower full-precision skip stays unconditional. Also: converts now name `<base>-mlx-<N>bit`. Plan: `docs/convert-quant-arch-gate-plan.md`.
- [`013-v1-inference-surface-api-control-only.md`](docs/decisions/013-v1-inference-surface-api-control-only.md) — **Accepted** (staged rollout): **`/v1` is the single inference surface; `/api/*` is the control plane** (model-store/RBAC/lifecycle/audit/usage/logs/cache). Deprecate native inference `/api/chat`+`/api/embed` (verified: `/api/embed` 0 callers, `/api/chat` only `athena run` → migrate to `/v1`); removal is breaking, gated on external-consumer confirmation. Also: audio division-of-labor (`/v1/audio/*`=analysis, chat parts=reasoning), add `diarized_json` response_format (#4a), honor `logprobs` on the greedy path (stop 400-ing), reclassify client/precondition faults from catch-all **500→cause-naming 4xx** (issue #4 no-chat-template + cluster #6: audio-upload faults, 4× rebind catch-alls, queue), keep per-request `enable_thinking` + add `reasoning_effort` alias on demand, and a **don't-build list** for the OpenAI platform tail (responses/batches/images/moderations/files/fine_tuning/assistants/legacy completions). Keeps `n>1`/`logit_bias`→400. Removal gate cleared (no first-party consumer; the downstream client self-migrates).
- [`014-speaker-identity-client-side.md`](docs/decisions/014-speaker-identity-client-side.md) — **Accepted** (decision; no daemon change): cross-file **speaker identity stays client-side** — the daemon is not extended (no enrollment store, no `/api/speakers`, no `/v1/audio/identifications`). It fails the ADR-011 governor test (cosine matching over 256-d WeSpeaker vectors is CPU bookkeeping — no Metal budget, no tenant to multiplex), has no cross-vendor standard to honor (ADR 013), and at ≤10 voices a local-JSON voiceprint store + brute-force cosine beats the built-in vector DB (single-dimension: 2560-d text vs 256-d speaker — can't coexist). Delivered as a self-contained **handoff prompt** (`docs/speaker-identification-agent-prompt.md` + `docs/speaker-identification-plan.md`). Tripwire: re-open if the roster grows past ~hundreds.
- [`015-block-until-ready-cold-load.md`](docs/decisions/015-block-until-ready-cold-load.md) — **Accepted** (shipped v0.10.164): a request for a **non-resident-but-on-disk** model now **blocks until loaded, then serves 200** (peer-runner behavior), instead of an immediate `503 module_loading`. Narrows M43.2's "never block on cold-load" rule to the *download* case — the request path never downloads (`MLXLLMModule` loads from a local dir; missing weights ⇒ 400; only the operator pull downloads). A separate `cold_load_wait_secs` budget (default 120; `0` = legacy-503 revert switch) bounds the wait → 503-fallback on timeout; downloads (`pulling`) still 503 at once; load failures surface their real error. Streamed requests emit `: loading` SSE keep-alives during the wait (model-dependent vision/prompt-cap checks then run in-band, OpenAI-consistent); non-streaming blocks silently. Governor `awaitLoad`/`peekLoad` are MLX-free + unit-pinned (ADR 009). Plan: `docs/cold-load-blocking-plan.md`.
- [`016-model-class-aware-convert.md`](docs/decisions/016-model-class-aware-convert.md) — **Accepted** (shipped v0.10.166): make `athena convert` **model-class-aware** so embedding models stop failing with opaque substrate errors. The substrate has three class factories (LLM/VLM/**Embedder**); convert hand-routed only LLM/VLM, so `bert` → `unsupportedModelType` and `gemma3_text` embedders → wrong-arch `keyNotFound`. New MLX-free `ModelClass` detector (config.json + sentence-transformers markers + embedder-only type set; unit-pinned per ADR 009) classifies generative/vision/embedding/unknown. **Redirect, don't convert** — convert is a generative/vision quant pipeline; an embedding model is **refused** with a cause-naming 4xx (`unsupported_convert_class`) pointing at `athena pull` + set-as-embedding-model (the serve path already loads it via `EmbedderModelFactory`). Detection runs on a config-only **pre-fetch** so the redirect fires before any weight download; substrate `unsupportedModelType`/`keyNotFound` on the generative/vision path is wrapped instead of leaked. Quantizing embedders via a third convert route = rejected/deferred. The "always chasing models" answer: *class* routing is finite/ours; *architecture* coverage is inherited from the substrate (best-effort loads regardless of the `SupportedModels` label). Plan: `docs/m73-model-class-routing-plan.md`.
- [`017-upload-limits-and-expect-continue.md`](docs/decisions/017-upload-limits-and-expect-continue.md) — **Accepted** (shipped v0.10.167): fix the "oversized audio upload hangs" report. Root cause is **`Expect: 100-continue`, NOT the size cap** — Athena/Hummingbird never send `100 Continue`, so any client that uses `Expect` (clients do for *large* bodies) deadlocks until its own timeout (reproduced for 1 MB **and** 30 MB; small files omit `Expect` so they work). Fix: a NIO `ExpectContinueHandler` (via `HTTP1Channel.additionalChannelHandlers`, both http1+TLS) answers `100 Continue` — or a final `413` when `Content-Length` already exceeds the cap. Separately: make caps **configurable + modality-scoped** — `max_audio_upload_bytes` (default **100 MiB**, up from 25) for `/v1/audio/*`, `max_request_body_bytes` (default 4 MiB) for JSON routes; `0`⇒parse-error not unlimited; over-cap returns a clean **413 `payload_too_large`** (was a 400 leaking `NIOTooManyBytesError`); cap surfaced in `/openapi.json`. Decision logic MLX-free in `AthenaServerKit`, unit-pinned (ADR 008/009). Research de-risked the interim-`100` write: the server codec passes informational heads through unconditionally, Hummingbird disables the fragile NIO state machines (`withPipeliningAssistance:false`/`withErrorHandling:false`), and the handler is stateless (~15 lines, send-100+forward) with over-cap rejection handled app-side regardless. Plan: `docs/upload-limits-plan.md`.
- [`018-multi-backend-diarization.md`](docs/decisions/018-multi-backend-diarization.md) — **Accepted** (shipped v0.10.168, fix v0.10.169): Sortformer is architecturally capped at 4 speakers, so add a **pyannote** backend behind `POST /v1/audio/diarizations` (`method=pyannote`) alongside `sortformer` (default, ≤4) and `cluster`. PyanNet (SincNet→4-layer BiLSTM→7-class powerset) ported to MLX from the ungated `aufklarer/Pyannote-Segmentation-MLX` safetensors; route does learned segmentation → WeSpeaker embed → **global** agglomerative cluster (same-window cannot-link) → overlap-aware turns with **file-stable** speaker ids; overlapping speech emits overlapping segments. Single global diarization slot (ADR 011), backend chosen by `config.json` model-class (MLX-free detector, ADR-016 pattern); method/model mismatch → 400 `invalid_method`. Whole-file in one call (~7.5× real-time/32 min). Auto speaker-count is approximate on long messy audio (a `min_cluster_size` reassignment tames it 92→~14); `num_speakers` forces exactly N via centroid reduction that overrides the cannot-link floor (v0.10.169 fix); `min/max_speakers`/`threshold`/`min_cluster_seconds` hints. Forward validated on real audio; decode/cluster/detector MLX-free + unit-pinned (ADR 008/009). Plan: `docs/multi-backend-diarization-plan.md`; usage: `docs/diarization.md`.
- [`019-parakeet-asr-deferred.md`](docs/decisions/019-parakeet-asr-deferred.md) — **Spike COMPLETE → GO** (M75): an MLX-Swift Parakeet-TDT-0.6B-v3 spike is **faithful** (verbatim-correct transcript on real audio) **and ~63× real time** (encoder ~529 ms + greedy TDT decode ~420 ms; 787 tok/s, Debug binary). The original "deferred for perf" basis was **corrected**: the MIT Swift-MLX reference was archived for an **iOS/ANE power-efficiency strategy** (mobile battery), irrelevant to a plugged-in macOS GPU daemon — so MLX-Swift Parakeet perf on a Mac was *unquantified, not known-bad*, and the spike resolved it decisively in favor. CoreML/ANE stays off the table (governor thesis), so MLX is the only path. Reuse win: encoder = Sortformer FastConformer (adapted keys + bias-off), pred-net LSTM = pyannote `Wx/Wh/bias` cell; net new = 128-bin mel + TDT joint/greedy decode + tokenizer. Spike code is additive/gated (`Sources/AthenaTranscription/Parakeet/*`, zero daemon impact). Decision: GO → full multi-backend transcription port (ADR 020). Interim `unsupported_transcription_arch` 4xx (v0.10.170) stays until the port lands.
- [`020-multi-backend-transcription.md`](docs/decisions/020-multi-backend-transcription.md) — **Accepted, shipped M76** (v0.10.171–175): transcription is **multi-backend, model-class routed** (Whisper + Parakeet), mirroring diarization's ADR 018. `TranscriptionArch` is now a positive router (`whisper`/`parakeet`/`unsupported` from `config.json` — reads NeMo `target`+`decoding.model_type` since mlx-community Parakeet has no top-level `model_type`); `MLXTranscriptionModule` holds either engine and dispatches by class; single governed `transcription` slot (ADR 011). **Whisper stays default; Parakeet additive** (selected by `model=`, seeded + `athena init`-pulled) — whether Parakeet *becomes* default is an **open decision** gated on a WER A/B (throughput already favours it ~75–135× RT). `/v1/audio/transcriptions` surface unchanged; Parakeet word/segment timestamps from TDT durations (0.08 s/frame) → verbose_json/SRT/VTT. Hardened from the ADR-019 spike: reference-exact **L1 mel magnitude** (`|re|+|im|`, not sqrt), SentencePiece special-token detokenizer, governed dispatch, long-audio chunking+stitch, anti-stall — all MLX-free decision logic unit-pinned (ADR 008/009), MLX numerics gated. No CoreML (thesis). S1 router, S2 engine, S3 timestamps, S4 chunking, S5 plumbing. Plan: `docs/parakeet-transcription-plan.md`; usage: `docs/transcription.md`.
- [`021-model-support-preflight.md`](docs/decisions/021-model-support-preflight.md) — **Accepted, shipped M77** (v0.10.176–179): unify model classification into one MLX-free `ModelSupport` predicate — *"what modality is this checkpoint + can Athena load it?"* — from **config-only** metadata, **composing** (not forking) `ModelClass`/`TranscriptionArch`/`DiarizationBackend` (all relocated to `AthenaCore` so one predicate can compose them) and adding the missing modalities (ASR/diarization/speaker-embedding) + **loadability signals** (whisper `n_vocab==51866`; parakeet NeMo `joint.vocabulary`). One predicate, three consumers so verdicts never drift: module loaders refuse unsupported packaging with a cause-naming 4xx (S2 subsumes the M76 Parakeet 500→400; transcription slot only — diarization's legacy no-`model_type`→Sortformer fallback left intact), `convert` redirects ASR/diarization/speaker/embedding models to `pull` (S3, instead of mis-routing Parakeet to the generative factory → misleading "bump the substrate"), and a new `pull` preflight (`--check` dry-run + config-only early-refuse/warn/proceed) lets the operator **confirm a model works before pulling it**, across every modality (S4). Honesty boundary: confirms routing/packaging, **not** numeric correctness (that stays the gated heavy tests). **Guidance rule (binding):** error/guidance strings name the *structural requirement* the checkpoint fails (e.g. missing `joint.vocabulary`), **never a hard-coded model id / HF repo** (repos move/vanish) — docs may cite repos for context, code must not. Motivated by an M76 field incident (nvidia vs mlx-community Parakeet). Plan: `docs/model-support-preflight-plan.md`; usage: `docs/model-support.md`.
- [`022-video-transcription-and-description.md`](docs/decisions/022-video-transcription-and-description.md) — **M78.1 shipped** (v0.10.183–186), M78.2 deferred: add **video** as orchestration over existing tenants, NOT a new model. Dedicated **Athena-native `/v1/video/*`** surface (per the `/v1` compatibility rule): `POST /v1/video/transcriptions` (M78.1 — `AVAssetReader` demux of the audio track → the Whisper/Parakeet transcription tenant unchanged → the OpenAI audio-transcription response shape) and `POST /v1/video/descriptions` (M78.2 — `AVAssetImageGenerator` frame sampling → per-frame Gemma4 VLM captions + optional LLM summary). **Keyframe-level only** (honesty boundary: what's on screen at sampled moments, not temporal — true video-LLM is substrate-gated). Composes at the orchestration layer, never at inference (ADR 011); one upload → governed audio + vision tenants on one Metal budget. Bounded by `max_video_upload_bytes` + a max-frame cap; passive-oracle (multipart only, no URL fetch); the audio track funnels through the shared decode floor (v0.10.182) so degenerate video = cause-naming 4xx, never a daemon abort. No new dependency (AVFoundation already linked). Plan: `docs/video-support-plan.md`.
- [`023-governor-memory-accounting-truthfulness.md`](docs/decisions/023-governor-memory-accounting-truthfulness.md) — **Accepted, ALL SLICES SHIPPED: G1 (v0.10.191), G3 (v0.10.192), G2 (v0.10.209–211)** (M79/M82): make the Metal memory governor's view **true** and **bound** the serve-path MLX buffer cache. Field finding: after heavy use `phys_footprint`=96 GiB (= the whole budget; Activity Monitor confirmed) = ~17 GiB active model weights + **~79 GiB reclaimable MLX cache**, while the governor believed only ~40 GiB resident (per-model estimates) — the long-deferred ADR-011 "governor accounting truthfulness" + heartbeat-RSS-undercounts-GPU gap. NOT a leak (reclaimable, budget-bounded); an accounting+control gap: the serve path sets no `MLX.Memory.cacheLimit` (convert caps at 256 MB), so the cache grows to fill the budget and peaked at 100 GB **over** budget, and admission overcommits because the cache is invisible to it. Fix (smallest-blast-radius first): **(1) ✅ bound the serve cache with a configurable `mlx_cache_limit_bytes` (G1 v0.10.191)**; **(2) ✅ G2 reconcile admission against the real footprint (v0.10.209–211) — denominator `max(committed, reserved)`, `committed = phys_footprint − reclaimable cache`, NOT estimates and NOT `activeMemory`** (the 2026-06-19 amendment corrected the probe: `activeMemory` misses mmap'd weights → M5.5 undercount; reclaimable cache is headroom, reclaimed-before-evict; reservation floor holds the warmup window; default-ON behind `governor_admission_mode` revert knob; `/healthz.admissionMode` + honest `freeBytes`); **(3) ✅ measure per-model footprint via a load-time RSS/footprint delta — honest `athena ps`/healthz (G3 v0.10.192)**. **Honesty boundary (binding):** per-tenant attribution of the reclaimable cache is gated by MLX (one global pool, no tenant tags), NOT us — we count+bound the cache (one global number) and measure *active* per-tenant bytes, never attribute the cache. Decision logic MLX-free + unit-pinned (ADR 008/009). Plan: `docs/governor-truth-plan.md`.
- [`024-in-memory-data-protection-coresident-threat.md`](docs/decisions/024-in-memory-data-protection-coresident-threat.md) — **[AMENDED publication S0 2026-07-07: T3 (idle-KV encryption) REMOVED — it rode the Qwen3.5-vendored-path-only M59 prompt cache; de-vendored + deleted, tracked mlx-tracker #37. T1 hardened-runtime + T2 ProcessHardening RETAINED.]** **Accepted — T1+T2+T3 ALL SHIPPED. T3 v0.10.206–208** (M81; encrypt idle prompt-cache KV at-rest-in-RAM, AES-256-GCM/swift-crypto, **RAM-only key/no-SEP**, ciphertext-at-rest-always via clone-on-hit, independent `prompt_cache_encrypt_idle` toggle off-by-default; justification leads with **swap/compressed-memory leakage** = only ciphertext ever swappable, the "anything that can reach swap" HIPAA/PCI story + why-T3-not-`mlock` for the idle pool, then residency-window collapse + DiD/attestation; net-new serialize seam (`KVFrame`+`KVByteCodec`) a future M59.5 reuses; **bit-identical gate PASSED with encryption ON on real 27B-4bit-mtp** (warm==cold byte-for-byte, restore beats cold re-prefill); honesty boundary unchanged — active working set+key+weights stay plaintext, no kernel/root defense; plan `docs/confidential-kv-cache-plan.md`, usage `docs/confidential-kv-cache.md`) (hardening program M80; **T1 v0.10.198**: Hardened Runtime + no `get-task-allow` via `deploy/build.sh` + `deploy/athena.entitlements`, fail-closed `deploy/verify-hardening.sh` gate, `NOTARIZE=1` notarization hook (Phase-0 Spike B proved task-port denial + metallib-loads-under-hardened-runtime, clearing M43.3); **T2 v0.10.200**: `ProcessHardening.swift` — `RLIMIT_CORE=0` always, opt-in `PT_DENY_ATTACH` (`deny_debugger_attach`/`ATHENA_DENY_DEBUGGER`), `secureZero`/`memset_s` of decoded PCM; `mlock` deferred (fights the governor, no clean small region)): capture the threat model + hardware ceiling for protecting the M59 prompt-cache/weights/KV **in memory** *before* a consumer asks for "confidential KV cache"/PCC-style protection. Reframes away from confidential-computing-vs-operator (wrong threat model for a local daemon; NVIDIA-CC's encrypted-VRAM+attestation primitive **does not exist on Apple Silicon** — unified memory, no confidential-Metal API; PCC's guarantees are an OS/boot/attestation posture we can't inherit as userland, and whether it uses MLX is unconfirmed+irrelevant) toward the real threat: a **co-resident malicious process** scraping our address space (`task_for_pid`→`mach_vm_read`). Baseline is the bottom of the ladder — `deploy/build.sh` ships **dev-signed (≈`get-task-allow`) = trivially debuggable**. Defense ladder (decision deferred, gated on the consumer's actual adversary): T1 **process lockdown** (hardened-runtime+notarized+no-`get-task-allow` → non-root can't `task_for_pid`, SIP-intact root substantially raised; costs the M43.3 metallib-bundling fix), T2 **side-channel hygiene** (`RLIMIT_CORE=0`, selective `mlock`, `PT_DENY_ATTACH`, best-effort zeroize-on-evict), T3 **encrypt idle prompt-cache KV at-rest-in-RAM** (AES-GCM/SEP, decrypt-on-restore only — collapses the plaintext window to the entry currently decoding). **Honesty boundary (binding):** the active working set (resident weights + the KV slice under live computation) is **irreducibly plaintext** — no encrypted-compute path on Metal; DRAM controller encryption defeats only physical/cold-boot, not a kernel read; a kernel/SIP-off adversary can always read it (same as keychain in-use secrets). Build-attestation + SQLCipher-at-rest (if M59.5 disk lands) tracked as separable adjacent work. Decision logic MLX-free + unit-pinned (ADR 008/009).
- [`025-collapse-persistent-data-tenants.md`](docs/decisions/025-collapse-persistent-data-tenants.md) — **Accepted — ALL SLICES SHIPPED** (M80, v0.10.199–205; **S5 in-memory decode v0.10.199**, **S1+S3 vector DB + `/v1/store*` removal v0.10.201**, **S7 allowlist retirement v0.10.202** (ADR 026), **S2 queue removal + model-ops→sync+SSE on `/api/models/{pull,convert,prune}` v0.10.203**, **S4 stateless loopback v0.10.204** — loopback+no-creds ⇒ no `athena.sqlite`, in-memory audit/usage, `persist_store` forces on-disk, decision pinned in `AthenaCore/StoreMode.swift`; **S6 surface/thesis reconciliation v0.10.205** — ADR 011/013 + Stable `/v1/*` list amended, model-op notice log, `deploy/e2e-rbac.sh` 500/0): minimize request-related data-at-rest (motivated by ADR 024). **Delete** the async queue (`jobs`) + built-in vector DB (`vectors`) **entirely** (code/tables/`/v1/queue*`+`/v1/vectors*` routes/DTOs/CLI/config/RBAC/tests/docs) + drop `/v1/store/export`+`/v1/store/stats` — operator-confirmed no consumer uses them, both are consumer-side concerns; delete-not-toggle so the persistence capability leaves the binary. Persistence audit: one `athena.sqlite` (`AthenaStore` actor), 8 tables/4 domains — queue+vectors are the only content-bearing **removable** leaf tenants (inference touches neither; sole coupling = model ops `pull`/`convert`/`prune` via `enqueueModelOp()`); the container stays for auth/audit/usage/allowlist. **Stateless loopback mode:** auth-off ⇒ create **no** `athena.sqlite` (audit/usage inert; allowlist retired per ADR 026) ⇒ zero request data-at-rest (upload temp files eliminated — see S5). **S5 = Option D (in-memory decode):** feed Apple's decoders from the in-memory upload `Data` via an `AVAssetResourceLoaderDelegate`-backed `AVURLAsset` (custom scheme) so `AVAssetReader` demuxes audio/video/future-frames with **no file on disk** — removes the upload crash-residue surface, same Apple decoder (no PCM-parity/codec/HW-decode loss), no new dependency, ~1–1.5wk after a ½-day resource-loader spike. **Rejected:** bespoke Rust/symphonia decoder (no cross-platform driver; owned codec/parity/CVE tax, ~2.5–4wk) and ephemeral-keyed encrypted volume (superseded — nothing to encrypt once no file is written). **Honesty boundary:** upload `Data` + decoded PCM still in process RAM → live co-resident read stays ADR-024 Tier 1; authed mode still persists auth/audit/usage by necessity. **Amends on acceptance:** ADR 011 (drop queue/vectors as governor tenants — thesis narrows to audio/embed/vision/video), ADR 013 + the Stable `/v1/*` list (breaking removal of 3 native surfaces), M42 (allowlist **retired**, superseded by ADR 026). Async-model-ops replacement RESOLVED = sync+SSE (S2). Plan: `docs/collapse-persistence-plan.md`.
- [`026-retire-allowlist-store-is-registry.md`](docs/decisions/026-retire-allowlist-store-is-registry.md) — **Accepted, SHIPPED v0.10.202** (M80; **supersedes M42's allowlist**): retire the `model_allowlist` SQLite table — *"the allow list should just be which models are pulled into the store."* The allowlist did 3 jobs; the store already answers 2: **availability = model-store contents classified by `ModelSupport` (ADR 021)**, **validation gate = store-presence + class match** (reuse case-insensitive basename `canonicalByStoreIdentity`; miss ⇒ 400 `model_not_available`; request path still never downloads). The 3rd job, the **default**, moves to **per-module TOML keys** (generalize `athena default` → `--module M`; `--*-model` load flags become first-boot config seeds). **Ambiguity rule:** omit `model` + >1 of class + no configured default ⇒ **400 `ambiguous_model`** (no silent auto-pick); exactly 1 ⇒ use it; 0 ⇒ `model_not_available`. Removes `/api/models/allow*` routes + WebUI mirrors + `athena allowlist` CLI (incl. M43.5 offline `--data-dir`) + DTOs + `model.allow.*` audit; M41 load/unload/resident + inference rebind stay, re-pointed at store models. Advances ADR 025: with no allowlist in SQLite, the DB is needed **only** for auth/audit/usage → loopback creates no DB unconditionally. ModelSupport (ADR 021) gains a 2nd consumer. Decision logic MLX-free + unit-pinned (ADR 008/009). Plan: `docs/collapse-persistence-plan.md` S7 (on approval).
- [`027-disk-kv-snapshots.md`](docs/decisions/027-disk-kv-snapshots.md) — **[AMENDED publication S0 2026-07-07: REMOVED — the disk-snapshot stack (`PrefixKVCache`/`KVFrame`/`KVByteCodec`/`KVSnapshot*`/`IdleKVCipher`) was Qwen3.5-vendored-path-only; de-vendored + deleted, tracked mlx-tracker #36 (gated on #24).]** **Accepted, S1–S4 SHIPPED v0.10.212–216** (S5 dropped, S6 deferred): revive **M59.5** as **versioned, encrypted, disk-backed KV snapshots** so a long session resumes across restart with **zero re-prefill** (motivated by a downstream client; reuses the ADR 024 T3 `KVFrame`/`KVByteCodec`/`IdleKVCipher` seam). **Proven on real 27B-MTP: bit-identical across a clean restart (`e2e-m59-disk-restart.sh`) AND survives SIGKILL via opt-in eager spill (`e2e-m59-disk-crash.sh`).** Transparent prefix-cache L2 (no new `/api` surface); **named snapshots dropped** (KV is prefix-keyed/prompt-matched — no session to load by name). Key = DEK/KEK envelope, **keyfile KEK shipped**, SEP-bound deferred (header reserves `kek_type`). Off by default + encryption mandatory (loopback writes nothing — ADR 025). Usage: `docs/kv-cache-disk-snapshots.md`. **Flat files in the data dir keyed by prefix hash, not SQLite**; **off by default** (loopback writes nothing — ADR 025 preserved), **encryption mandatory** when on. Key = **DEK/KEK envelope with a swappable KEK** (`keyfile`/`passphrase` now → `sep`-bound later, drop-in; per-peer ECIES multi-node wraps reachable) — see the **ADR 024 amendment** (re-opens SEP per its own *"only if M59.5 persists the key"* condition). Self-describing header (`format_version` decoupled from `appVersion`, `save_reason`, model/quant bits → **skip-on-skew**). Triggers = **auto (idle-evict demote-to-disk + SIGTERM flush) + frontier-interval** continued saves. Surfaces = **transparent prefix-cache L2 restore** + **explicit named snapshots** (`athena cache save/load`, `/api/cache/*`). Governor gains an **L2 disk tier** (disk ≠ Metal bytes; retention LRU). **Correctness bar = bit-identical across a real process restart** (model/quant mismatch ⇒ skip-restore). Decision logic MLX-free + unit-pinned (ADR 008/009). Plan: `docs/kv-cache-disk-snapshots-plan.md` (on approval).
- [`028-retire-dflash-turboquant-upstream-sync.md`](docs/decisions/028-retire-dflash-turboquant-upstream-sync.md) — **[AMENDED publication S0 2026-07-07: the "Kept: TriAttention eviction (Athena-vendored)" line is NO LONGER TRUE — the whole vendored `AthenaModels` (Qwen3.5 base + fused MTP head + GatedDelta + TriAttention) was de-vendored + deleted; substrate 751aaed exports `Qwen35Model` + `Qwen35MTPDraftModel` + `MLXLMCommon.TriAttention*`; TriAttention now rides `GenerateParameters.kvScheme`. Bit-identical verified.]** **Accepted, SHIPPED v0.10.217** (supersedes ADR 001): hard-remove **DFlash** (ADR 001) **and TurboQuant** (M20) so the substrate fork can be consolidated to track upstream `mlx-swift-lm`. Both bound to bespoke substrate deltas dropped from the active `integration` branch — DFlash's `Gemma4TextModel.callReturningHidden` capture seam (`parked/dflash`) and TurboQuant's `KVQuantizationScheme` enum (`parked/turboquant`, superseded upstream by #230's `kvScheme: String?`). Athena referenced both unconditionally ⇒ **didn't compile against `integration`** until removed (forced, not optional). Both default-off, no external consumer. Deletes the whole `Sources/AthenaModels/DFlash/` + `DFlashGeneration` + `MLXLLMModule` DFlash wiring + `dflash_enabled`/`ATHENA_DFLASH`; drops the `KVCompression.turboquant` case (knob now `none`|`triattention`; `turboquant` fail-closes at start). **Kept:** the MTP speculative path (M40), the per-request `speculative` knob, ~~shared `SpeculativeStats`~~ (**deleted 2026-08-01 by issue #47** — publication S0 removed its only publisher; ADR 032 S4 stats ride the substrate's aggregate counts), and TriAttention eviction (Athena-vendored, pristine `KVCache`). Recoverability = git history/tags + substrate `parked/*` + `bstnxbt/dflash-mlx`. Revival belongs upstream (generic capture seam; TurboQuant as a core `mlx` quant mode via `kvScheme`). See `~/Source/mlx/research/upstreaming-audit.md` + substrate ADR 0001/0002.
- [`029-execution-exclusive-inference-slot.md`](docs/decisions/029-execution-exclusive-inference-slot.md) — **Accepted** (from a 2026-06-26 full code review; clarifies ADR 011's "single slot"): the governor today enforces the single slot only as **one memory reservation per module class**, not execution exclusivity, so an LLM decode + an audio transcription (or a warm rebind racing an in-flight decode) drive MLX kernels on the one Metal pool **concurrently** — the "never compose at inference" hazard ADR 011 forbids, and a likely cause of the 2026-06-05 GPU-wedge. Decision: add **one process-global inference execution gate** (semaphore/serializing actor) every Metal-executing op **and** model rebind/load-swap acquires for its execution span (released on throw/cancel); composes with but is separate from the `MemoryGovernor`. **Subsumes H3** (warm-rebind double-residency — rebind serializes against decode, no module-local serializer needed) and resolves **M1** (a `Task.isCancelled` backstop + test in the decode loops). Behind a default-on revert knob. Decision logic MLX-free + unit-pinned (ADR 008/009).
- [`030-default-prefill-ceiling-and-mlx-fault-degrade.md`](docs/decisions/030-default-prefill-ceiling-and-mlx-fault-degrade.md) — **Accepted; Part 1 IMPLEMENTED** (from the 2026-06-26 review): an oversized chat prompt drove the O(seq²) prefill score buffer past the device's max Metal buffer → substrate `[metal::malloc]` throw on the MLX worker thread → global `setErrorHandler` re-`fatalError` → **whole-daemon abort** (the `max_prompt_tokens` guard existed but was default-off, and the substrate-stream path bypassed it entirely). **Part 1 (shipped):** default-on derived ceiling — `GovernorMemory.defaultPromptTokenCeiling(maxBufferBytes:)` (conservative `sqrt(maxBuffer/128)`, MLX-free + unit-pinned) enforced at **both** `container.prepare` chokepoints (`runSpeculative` + `beginGeneration`); operator `max_prompt_tokens` override retained (explicit `0` = unbounded opt-out). **Part 2 (staged follow-up):** degrade a recognized `[metal::malloc]`/`maximum allowed buffer size` fault (the H2 `isMetalOOM` needle fix already shipped) to a classified 503 instead of re-fataling. Mirrors the embedding NI4 always-on ceiling.
- [`031-remove-deprecated-api-chat.md`](docs/decisions/031-remove-deprecated-api-chat.md) — **Accepted** (executes ADR 013; from the 2026-06-26 over-engineering review): remove the deprecated **`/api/chat`** native inference surface — `handleNativeChat` + `textChunks` + `AthenaChatRequest`/`Response`/`Chunk` DTOs (~120 LOC) — a full parallel inference impl duplicating `/v1/chat/completions` (a defect per the "no parallel pipeline" rule) with **zero live callers** in `Sources/`/`clients/`. Drop the `/api/chat` path from `OpenAPISpec.swift` (SSOT) + the e2e drift-guard in the same edit. Breaking surface change, gated on external-consumer go/no-go (ADR 013 indicated none; `athena run` already on `/v1`). `/api/embed` already de-duplicated (shared `governedEmbed`) — no inference code to delete there; alias removal optional. `/api/*` stays control-plane only.
- [`032-gemma4-mtp-speculative-decoding.md`](docs/decisions/032-gemma4-mtp-speculative-decoding.md) — **Accepted, IMPLEMENTED** (M83; S1 v0.10.227, S2–S5 v0.10.228; heavy E2E DoD pending): wire **Gemma 4 MTP** speculative decoding as a **second drafter backend behind the existing per-request `speculative` knob** — multi-backend, not a parallel pipeline (ADR 018/020 precedent; the Qwen3.5 fused-`mtp.*`-head path is byte-unchanged). Gemma 4 MTP is structurally different: a **separate `gemma4_assistant` drafter checkpoint** (shares target embeddings + last-layer activations), and the **substrate owns the decode loop** — the mlx-swift-lm `integration` branch ships the drafter, `MTPDrafterModelFactory`, the `emitDrafterState` capture seam, and a `generate(… mtpDrafter:)` overload (substrate-first per ADR 028). The target's `config.json` does **not** advertise its drafter (no `mtp_*` field, unlike Qwen) and the drafter is a **separate HF repo**, so pairing can't be sniffed: it resolves explicit `mtp_drafter` config key > a **seeded, operator-overridable default-drafter map** (`Sources/AthenaCore/Resources/mtp-drafters.toml`, keyed by target basename → drafter HF id) > none (knob inert). New `ModelModality.mtpDrafter` (ADR 021) — pulled (bf16) not converted (`convert` redirects), never an auto-served slot. Two resident models accounted on one Metal budget (ADR 023); one inference-execution span (ADR 029). Default-off, opt-in, lossless (substrate target-verify; byte-identity bounded ~64 tokens by an MLX-SDPA quirk; **speedup measured not promised** — 26B-A4B MoE may not help at batch-1). **ADR 021 D5 surfaced+resolved:** the repo-id map ships as **config data, not Swift constants** (operator fixes a moved repo without recompile). Slices S1 (classification/config/map) ✅ → S2 (load+pair+governor) → S3 (drive overload) → S4 (stats) → S5 (surface+docs). Plan: `docs/gemma4-mtp-plan.md`; handoff: `docs/mtp-gemma4-handoff.md`.
- [`034-tool-choice-auto-non-forcing.md`](docs/decisions/034-tool-choice-auto-non-forcing.md) — **Accepted, IMPLEMENTED v0.10.231**: OpenAI `tool_choice:"auto"` (the default) must **not** force a tool call. Bug: `selectedTools()` collapsed `auto`/absent/`required` into "force", so `effectiveSchema()` engaged the structured-output Guide and grammar-masked the model into emitting a tool-call object **every turn** — it could never produce the final text answer, so a client re-sending tool results looped forever (the gemma-4 studio tool spiral; the v0.10.230 streaming-`tool_calls` fix merely let the client *see* the calls and start the loop). Fix (both halves, no revert knob, trust substrate detection): a pure `resolveToolChoice` algebra (`AthenaServerKit`, unit-pinned) makes `auto`/absent **advertise the tool menu but force nothing** — the model decides; `required`/named still force the Guide; `none` withholds the menu. An un-forced free tool call is detected by the **substrate's native `.gemma4` handler** (`Generation.toolCall`, previously dropped at `MLXLLMModule`'s `default: break`) → new `GenChunk.toolCall` → serialized as OpenAI `tool_calls` + `finish_reason:"tool_calls"` in BOTH the non-streaming collect and the streaming pump (reusing the v0.10.230 delta shape). Secondary: tool-turn **history was lossy** (`ChatTurn`/`chatMessages` carried only role+content, dropping assistant `tool_calls` + tool `tool_call_id` and *dropping the assistant tool-call turn entirely* when content was null) — now carried onto the substrate `Chat.Message.tool` so the model sees a coherent call→result history and answers instead of re-calling. Honesty boundary (binding): `auto` tool **detection is substrate-arch-gated** (gemma4 wired; an arch with no inferred `toolCallFormat` may under-detect — use `required` there). Decision logic MLX-free + unit-pinned (ADR 008/009). DoD = `deploy/e2e-tool-choice-auto.sh` (studio, gemma-4-26b-a4b-it-8bit). Plan: `docs/tool-choice-auto-plan.md`; usage: `docs/tool-calling.md`.
- [`035-reasoning-channel-to-reasoning-content.md`](docs/decisions/035-reasoning-channel-to-reasoning-content.md) — **Accepted, IMPLEMENTED v0.10.232**: route channel-delimited reasoning to OpenAI `reasoning_content`. `gemma-4-26b-a4b-it` (+family) emit a thinking channel as **literal text** — `<|channel>thought\n…\n<channel|>…content…` (tokenizer_config `soc_token`/`eoc_token` + an `x-regex` splitting `thinking`/`tool_calls`/`content`); the template **forces the channel on whenever `tools` are present** (so `enable_thinking=false` can't suppress it). The substrate's `.gemma4` `ToolCallProcessor` strips tool-call markers but **not** the thought channel, so it leaked into `content` — exposed by ADR 034 (the old forced path masked all output to pure tool-JSON; free generation surfaces the native format; it's a **pre-existing free-gen gap**, not an ADR 034 regression). Fix: a pure MLX-free `ReasoningChannelFilter` (`AthenaServerKit`, unit-pinned) — `splitReasoningChannel` (non-stream) + a streaming splitter mirroring `StopStreamFilter`'s partial-marker holdback — peels `<|channel>…<channel|>` blocks (dropping the `thought\n` header) and surfaces them as `reasoning_content` on `ChatMessage` (non-stream) / `ChatDelta` (stream), both omitted-when-nil so plain responses are byte-unchanged. **Universal, not model-gated** (keys on the literal markers, which don't occur in normal text → no-op elsewhere; upgrade path = read `soc_token`/`eoc_token` from tokenizer_config). Operator chose `reasoning_content` over dropping (preserves chain-of-thought). Decision logic MLX-free + unit-pinned (ADR 008/009). Verified: e2e check 2 now also asserts no `<|channel>` leak in `content`.
- [`036-multi-protocol-adapters-over-one-engine.md`](docs/decisions/036-multi-protocol-adapters-over-one-engine.md) — **Accepted; S1 seam SHIPPED v0.10.233, S2 Anthropic `/v1/messages` SHIPPED v0.10.234 (non-stream) + v0.10.235 (streaming); e2e 5/5; S3 real Claude Code VERIFIED end-to-end v0.10.236 (no proxy, bearer auth, `docs/claude-code.md`)** (amends ADR 013/031 + the `/v1` tagging rule on ship): serve multiple API dialects as **concurrent compiled-in protocol adapters over one inference engine**, not a swappable plugin and not a parallel pipeline. Driver: Claude Code speaks the **Anthropic Messages API** (`POST /v1/messages`), which Athena lacked, forcing an Ollama shim. An adapter is **decode+encode only** (maps a dialect ⇄ native `ChatTurn`/`GenChunk`); the shared orchestration (admission/cold-load/vision/preflight/metering/cancel) is extracted into a **seam** — which is what keeps it *legal* under ADR 031 (the rule is "no parallel **implementation**," a translation layer over the same engine complies). ADR 013 restated: single inference **engine**, surfaced in multiple protocols (not "single surface"). Also **collapses the streaming/blocking drift**: one `AsyncStream<GenChunk>` + two generic terminal ops (`drain` blocking / `forward` SSE) parameterized by a per-protocol encoder — tool-call **detection** + cancel/metering/finish live **once** (closes the v0.10.230 streaming-`tool_calls` and A8/E3/E13 streaming-cancel drift class); transport-intrinsic surfacing (cold-load `: loading` keep-alives ADR 015; mid-stream error → HTTP status vs in-band) stays per-mode. **Namespace policy:** idiomatic path where free (Anthropic `/v1/messages` collides with nothing), explicit **mount-prefix** for colliders (Ollama `/api/*` would fight the control plane) — base-URL-configurable harnesses make a static prefix free, all dialects + control plane live at once. New tag `[anthropic]` (third-party-protocol-compatible) alongside `[oai]`/`[native]`. First cut = what Claude Code drives (text/system/tools/streaming/stop+usage; `x-api-key` bearer alias); image/document blocks, prompt-caching, extended-thinking, Ollama deferred. Rejected: swappable plugin (disables control plane, one-at-a-time, mutable state), dynamic plugin runtime (ceremony for a small owned set), LiteLLM-style proxy (extra process breaks single-binary passive-oracle; fastest interim unblock only). Slices: S1 seam+collapse (OpenAI byte-unchanged, pinned by `e2e-tool-choice-auto.sh`) → S2 Anthropic encoder+`/v1/messages` → S3 `x-api-key` + real-Claude-Code e2e. **Native-capability placement (three-rung rule, §7):** knob on an existing call → tolerated extension field mapped onto `NativeChatRequest` (each adapter exposes it in its own idiom); whole capability with no vendor shape → `[native]` `/v1` endpoint (diarization/speaker/video — the existing pattern, never removed); daemon control → `/api/*`. No native-namespace/extensions framework built ahead of a named feature (YAGNI). Decision logic MLX-free + unit-pinned (ADR 008/009). Plan: `docs/anthropic-adapter-plan.md` (on approval).
- [`037-daemon-mediated-config-and-restart.md`](docs/decisions/037-daemon-mediated-config-and-restart.md) — **Accepted** (usability audit 2026-07-02 §6; brownfield-gated, operator-approved 2026-07-03): make config + restart **daemon-mediated** so `sudo` is needed **only** for `install`/`uninstall`. Root cause: `LaunchdPlist.dictionary` freezes ~30 TOML values into the plist `ProgramArguments` (so any config change = root plist re-render) and the installed TOML is `root:wheel`, which also breaks the daemon's OWN config-write surfaces (WebUI `POST /ui/api/config`, `POST /api/models/default` → `writeFailed`). Fix (3 slices): **(1) static plist** (label/user/exec/`ATHENA_CONFIG` only) + `athena load --background` reads the full TOML at boot (flags still win); **(2) chown** the installed TOML to the service user + `GET`/`PUT /api/config` (control plane per ADR 013, `daemon.admin`, audited, spec same-edit) wrapping the hardened `ConfigEditor`, `athena config set`/`default` repointed at the API with sudo fallback; **(3) `POST /api/admin/restart`** (`daemon.admin`, audited, drains via the ADR 029 gate then `exit(0)`; `KeepAlive` relaunches). **Security decision (binding):** a `PUT /api/config` **deny-list** — `auth_keys_file`/`tls_cert`/`tls_key`/`encrypt_store`/`data_dir`/`deny_debugger_attach` are TOML-plus-sudo only (config takeover ≈ daemon takeover, and loopback dev mode has no auth). Migration: the plist is re-rendered once to the static form on next install/upgrade; `KeepAlive=true` is already live so sudoless restart needs no plist change. Decision logic MLX-free + unit-pinned (ADR 008/009).
- [`038-serialize-execution-batch-within-the-span.md`](docs/decisions/038-serialize-execution-batch-within-the-span.md) — **Accepted; immediate slice SHIPPED v0.10.263** (multi-inference audit 2026-07-04, `docs/parallel-inference-audit-2026-07-04.md`): inference **stays serialized** behind the ADR 029 gate, with the rationale **corrected on record** — batch-1 decode is memory-BANDWIDTH-bound (~1–4 FLOP/byte vs ~60 ridge; GPU ~95% compute-idle), so serialization is right because the substrate has no batched decode loop (mlx-swift-lm #42 open), cross-modality can't batch, and MTP speculative is the batch-1 optimization — NOT because "Metal has fewer cores". Sanctioned upgrade = **continuous batching INSIDE one gated span** (ADR 029's "one eval graph in flight" already permits it), substrate-gated + evidence-gated (trigger: routine ≥2-deep chat contention on gate metrics); blocker list incl. per-sequence KV reservations in the governor. Cross-tenant execution overlap stays **forbidden** (process-global MLX allocator/cache/fault state; wedge history). Immediate slice **SHIPPED v0.10.263**: **InferenceGate observability** — `gateWaiters`/`gateHeld`/`gateHeldMs`/`gateMaxWaiters`/`gateWaitP95Ms` on /healthz + `athena_inference_gate_*` on /metrics (was test-only; FIFO starvation now visible; trigger = routine `gateWaiters ≥ 2`). Rejected: concurrent unbatched decodes (splits bandwidth, blinds governor), foreign batching engine behind the module protocols (forfeits the ADR 011 thesis). **Amended 2026-07-05:** the substrate batch loop **LANDED** — mlx-swift-lm **#263** (continuous batching) ported to fork `integration` (`BatchGenerator`/`BatchKVCache`/per-row samplers/batched prefill; #42 superseded). Trigger flips to gating the *integration* work; re-scoped remaining = substrate gaps (paged-KV, batched-attention Metal kernel, batched-speculative, per-row grammar hook, MoE-batch verify) + Athena wiring (governor per-seq KV accounting [load-bearing], gate→`BatchGenerator` scheduler, route plain requests, per-row structured output — **none wired yet**). See the ADR 038 amendment.
- [`039-per-sequence-kv-accounting-and-batch-scheduler.md`](docs/decisions/039-per-sequence-kv-accounting-and-batch-scheduler.md) — **Accepted; S0+S1+S2 SHIPPED v0.10.264–266 (batching default-off, verified e2e on gemma-4-26b-a4b)**: executes ADR 038 Decision 2 as **minimal core, default-off**. Extends the governor with **per-sequence KV accounting** (conservative worst-case `maxTokens × perTokenKVBytes`, admitted against the ADR-023 truthful budget — the load-bearing gap; deny a batch seat rather than risk mid-batch OOM) and adds **one batch scheduler** owning a `BatchGenerator` and holding **one ADR-029 gated span** (one batched `next()` = one eval graph, so ADR 011/029 hold unweakened), fanning per-row tokens into each request's `AsyncStream<GenChunk>`; `rebind` = batch barrier, same-model only. Behind `batching_enabled` (default off; flip when `/metrics` shows routine `gateWaiters ≥ 2`). Only **plain unstructured chat** batches — structured/MTP/speculative/logprobs stay single-row via the existing `runSpeculative` fork. Deferred: per-row structured output (substrate `RowSampler` grammar hook), paged KV (#12/#13, de-risked academic on large unified memory), shared-prefix dedup (#16). Slices: **S0 substrate re-pin (v0.10.264)** → S1 governor per-seq KV → S2 scheduler+routing+e2e. Decision logic MLX-free + unit-pinned (ADR 008/009). Plan: `docs/batching-integration-plan.md`. Capacity de-risked 2026-07-05 (~3× at N=8, plateau ~18×/13.5× N=64 dense/MoE; [[project_batching-capacity-spike]]).
- [`040-publish-athena-as-a-personal-identity-public-monorepo.md`](docs/decisions/040-publish-athena-as-a-personal-identity-public-monorepo.md) — **Accepted** (2026-07-07): publish Athena as a **personal-identity public monorepo** — NO core/macOS/Linux/client repo split (Linux lands later as platform seams in this repo, gated on the CUDA-audit spike; any OS-split proposal must supersede this ADR); `GoodOlClint` GitHub + individual Apple Developer cert + bundle id `me.goodolclint.athena` kept (LLC migration path reserved; never hard-pin the Team ID in self-update); the parent-project name is dropped as a **consumer reference** (consumers are never referenced — now generalized); history via **filter-repo string-scrub, timestamps untouched, filter → soak → flip** (original repo archived read-only, never pushed again); **public-primary** development with an AI-authorship README disclosure; **Apache-2.0 + NOTICE**; versioning re-baselined **v0.11.0 at the flip, versions = release events** (per-slice bump ritual retires then); ADRs stay public (scrubbed) + retrospective `000-pre-adr-history.md`; internal docs → private repo; signed/notarized GH Actions release pipeline. Plan: `docs/publication-plan.md`; decision record: `~/.athena-publication/publication-and-repo-split-brainstorm.md` (outside the repo — names scrub terms).
- [`041-per-principal-token-budgets.md`](docs/decisions/041-per-principal-token-budgets.md) — **Accepted, SHIPPED A1–A4 2026-07-25** (executes ADR 007 #9, closes #8 by obsolescence; usage `docs/token-budgets.md`, DoD `deploy/e2e-token-budget.sh` 31/0): per-principal **token budget over a rolling period**. `usage_counters` gains additive `period_start`/`period_prompt_tokens`/`period_completion_tokens` (existing columns keep their shipped meaning as **lifetime** totals — a period roll destroys no history); the roll is **lazy pure arithmetic** (`QuotaWindow.periodStart(containing:window:)`), no timer/job. Budget = global `token_budget` + `token_budget_window` (`day`|`month`) TOML default, overridable per user via a nullable `auth_users.token_budget` (NULL ⇒ inherit), set on the user-admin control plane + CLI, audited; **not** ADR-037 deny-listed. Enforcement = a `QuotaMiddleware` mirroring `RateLimitMiddleware` (auth-gated, principal-keyed) returning **429 `insufficient_quota`/`quota_exceeded` + `Retry-After`**, scoped to the **token-bearing routes only** (chat completions, embeddings, messages — deliberately NOT `throttled()`, so an exhausted principal can still read `/api/usage` and diagnose), plus advisory `x-athena-tokens-{limit,remaining,reset}` on normal responses (omitted entirely when unlimited — absent ≠ zero). `/api/usage` + `athena usage` gain `budget`/`period_tokens`/`period_reset` (omitted-when-nil). **Honesty boundary (binding):** a request's cost is unknown before it runs, so enforcement is pre-request against *accrued* usage — a principal with budget left is admitted and **may overshoot by one request** (bounded by the prefill ceiling + `max_completion_tokens`); mid-stream cancellation is rejected, not deferred. **Quotas are inert in loopback dev mode** (ADR 025: no store, no principal — same bypass the rate limiter already has). Decision logic MLX-free + unit-pinned (ADR 008/009). Plan: `docs/token-budget-and-context-plan.md`.
- [`042-context-window-discovery-and-token-counting.md`](docs/decisions/042-context-window-discovery-and-token-counting.md) — **Accepted, SHIPPED B1–B3 2026-07-25** (DoD `deploy/e2e-count-tokens.sh`; count == usage.prompt_tokens exactly, tool schemas included. **§4(b) amended on measurement:** counting is ~38 ms idle and takes no ADR 029 gate, but it is NOT concurrent with a decode — `ModelContainer.prepare` needs the substrate's `SerialAccessContainer` mutex, held for the whole generation, so a mid-decode count waits it out): let a client compute **context room remaining**, which today is impossible without guessing — the real limit is `min(model context window, Athena's ADR-030 prefill ceiling)` and **neither number was discoverable** on the inference surface (`/v1/models` returned only `{id,object,created,owned_by}`; the ceiling surfaced only by tripping 400 `input_too_long`), while `usage.prompt_tokens` measures only *after* a round trip. Three parts: (1) `ModelConfigInfo` gains `maxPositionEmbeddings` (top-level then `text_config`, nil when absent); (2) `GET /v1/models{,/id}` gains omitted-when-nil `context_length` + effective `max_prompt_tokens` — **Athena-native extension fields on an `[oai]` route**, marked as such per the `/v1` compatibility rule (both inputs published, not a pre-mined "remaining", because the two limits fail differently: model-capability error vs memory guard); (3) new `POST /v1/chat/completions/count_tokens` **[native]** returning `{prompt_tokens}` computed by **the request path's own `container.prepare`** — same template/tokenizer/tool serialization — so a preflight count equals the subsequent request's `usage.prompt_tokens` **by construction** (verified: for text-only input both the LLM and Gemma4-VLM processors are `messageGenerator` → `applyChatTemplate` → `MLXArray`, so reading `.shape` **evaluates nothing** ⇒ no prefill, no kernel, and — deliberate narrow exception with a recorded tripwire — **no ADR 029 gate**, so an every-turn count never queues behind another user's decode). Obeys ADR 015 cold-load (**counting can trigger a load**); **neither metered nor quota-enforced** (it exists so a client stays under its ADR 041 budget); image-bearing counts ⇒ 400. **Amended same-day** on a downstream consumer's requirement spec (§4): OpenAI dialect is primary (the "defer until an OpenAI-dialect consumer asks" tripwire fired) and the Anthropic `/v1/messages/count_tokens` is **deferred** (same core + existing decoder); `max_output_tokens` **refused** — `max_tokens` config is a *default*, not a cap, and publishing it would mislead a budgeting client; post-turn `stream_options.include_usage` **already ships** (M27.4, `AthenaServer+SSE.swift:388`) so that ask needs no work. Rejected: standalone-tokenizer count (drifts from the serving path), `/v1/tokenize` as a name (implies token ids, which we don't return), char heuristics, control-plane-only raw `config.json`, gating the count. **Honesty boundary:** `context_length` is what the checkpoint *declares* — missing or optimistic configs pass through unvalidated (same boundary as ADR 021) — and the ADR-030 derived ceiling is often **an order of magnitude lower** than it (`sqrt(maxBufferSize/128)` ≈ 17k tokens on a 48 GB device vs a 128k-advertising checkpoint), so the effective budget is the **min** and the operator should set `max_prompt_tokens` deliberately rather than inherit an OOM-safety floor as context policy. Plan: `docs/token-budget-and-context-plan.md`.

- [`043-adopt-swift-sqlcipher-package-over-vendored-sqlcipher-amalgamation.md`](docs/decisions/043-adopt-swift-sqlcipher-package-over-vendored-sqlcipher-amalgamation.md) — **Proposed** (operator review pending): replace `Sources/CSQLCipher` (vendored SQLCipher 4.6.1 amalgamation, CommonCrypto — M34.3a) with the `skiptools/swift-sqlcipher` package's raw-C `SQLCipher` product (4.17.x, LibTomCrypt, source-built, cross-platform incl. Linux), **exact-version pin, deliberate bumps**, landing before v0.11.0. On-disk format is provider-independent + stock PRAGMAs only ⇒ existing stores open unchanged; key management untouched (RAM-only `sqlite3_key`, no SEP either side). Accepted price: software AES (perf-negligible on sparse control-plane rows; side-channel class out of ADR 024 scope). Kills the Linux port's CC→OPENSSL work item. Verification bar: NDEBUG check (PR upstream if absent), bidirectional encrypted-store compat, e2e §27 + unit tier green.

## Build / run / test

```sh
./deploy/build.sh Release           # xcodebuild → .build/xcode/.../Release/athena
./deploy/test.sh                    # unit tests
swift format --in-place --configuration .swift-format --recursive Sources Tests clients Package.swift
                                    # style gate (CI lints with swift-format pinned at 6.3.3)
./deploy/e2e-rbac.sh                # RBAC end-to-end
athena load                         # run daemon in foreground on loopback (no auth)
curl http://127.0.0.1:7447/healthz  # liveness
curl http://127.0.0.1:7447/openapi.json  # self-describing surface
```

For production install (boot-time launchd, TLS, bearer auth, WebUI), see `docs/quickstart.md`.

## Versioning (ADR 040 S8 — in force since v0.11.0)

**Versions are release events, not commit events.** `Athena.appVersion` (`Sources/athena/Athena.swift`, SSOT — also stamped into `/openapi.json`) bumps only when a release is cut: **patch** = fix-only release, **minor** = feature release, **major/1.0** = operator-reserved. Slices land as plain commits with NO version bump and NO per-slice tag (the pre-publication bump-every-slice ritual is retired). Annotated `v*` tags mark releases and drive the release pipeline. Do not bump `appVersion` in a feature commit — bump it in the release commit the operator approves (`/ship`).
